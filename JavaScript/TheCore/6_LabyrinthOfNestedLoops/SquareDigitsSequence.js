function squareDigitsSequence(a0) {
    let lib={};
    let counter=0;
    let currentNum=a0;

    while (lib[currentNum]==undefined){
        lib[currentNum]=1;
        
    }
}

let n=16;
console.log(squareDigitsSequence(n));